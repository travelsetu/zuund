# Keep workspace packages linked via the workspace: protocol.
link-workspace-packages=true
prefer-workspace-packages=true
# Hoist type packages so editors resolve them from the root.
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*prettier*
