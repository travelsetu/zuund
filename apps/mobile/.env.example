# Copy to .env. Public vars are inlined into the bundle.
# Leave empty in development: the app derives http://<this Mac's LAN IP>:3000 from the Expo dev server.
EXPO_PUBLIC_API_URL=
# Production builds: EXPO_PUBLIC_API_URL=https://api.zuund.com
